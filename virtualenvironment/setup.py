from setuptools import setup, find_packages

setup(name='draw-something',
      description='Toy project to learn Python',
      long_description=open('README.md').read(),
      author='',
      author_email='',
      url='',
      scripts=['bin/draw'],
      packages=find_packages(),
      classifiers=[])